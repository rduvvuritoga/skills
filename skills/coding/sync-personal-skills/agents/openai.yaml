interface:
  display_name: "Sync Personal Skills"
  short_description: "Manually sync Ravi’s global skills"
  default_prompt: "Use $sync-personal-skills to sync my global skills from the skills repository."
policy:
  allow_implicit_invocation: false
